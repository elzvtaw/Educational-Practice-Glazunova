#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

int main() {
  int n = 0;
  int k = 0;
  std::cin >> n >> k;
  std::vector<int> p(n);
  for (int i = 0; i < n; ++i) {
    std::cin >> p[i];
  }
  std::string s;
  std::cin >> s;

}